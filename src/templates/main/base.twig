<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">

    <title>{% block title %}{% endblock %}</title>

    <!-- Bootstrap core CSS -->
    <link href="/src/templates/main/css/bootstrap.min.css" rel="stylesheet">
    <link href="/src/templates/main/css/style.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>

  <body>

    <div class="container">
      <div class="header clearfix">
        {% block top_nav %}
        
        <nav>
          <ul class="nav nav-pills pull-right">
            {% for name, url in menu %}
              {% set classValue = "" %}
              {% if request.url == url %}
              {% set classValue = "active" %}
              {% endif %}
              <li role="presentation" class="{{classValue}}"><a href="{{url}}">{{name}}</a></li>
            {% endfor %}
          </ul>
        </nav>
        {% endblock %}
        <h3 class="text-muted">Flight skeleton</h3>
      </div>

	  {% block content %}{% endblock %}

	  {% block footer %}
      <footer class="footer">
        <p>&copy; 2016 AndreyLeo.</p>
      </footer>
	  {% endblock %}

    </div> <!-- /container -->
  </body>
</html>
